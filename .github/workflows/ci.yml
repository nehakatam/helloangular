name: Angular-DEV-CI-CD

on:
  push:
    branches:
      - sri

jobs:
  ci:
  
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2
        with:
          token: ${{secrets.PAT}}
      - run:  |
          git push
