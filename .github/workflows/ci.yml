name: merge conflicts

on:
  push:
 # pull_request:
    paths-ignore:
      - '.github/workflows/ci.yml'
      #- '**/.github/workflows/**'
    
  
jobs:
  ci:
    runs-on: windows-latest
       
    steps:
       - name: checkout code
         uses: actions/checkout@v2
         
       
       
       
          
    
       
   
      
