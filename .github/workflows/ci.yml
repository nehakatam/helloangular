name: Cron Job for automatic

on:
  schedule:
    - cron:  '*/6 * * * *'
    
  
jobs:
  ci:
    runs-on: windows-latest
    
    
    steps:
       - name: checkout
         uses: actions/checkout@v2
         
  
    
       
   
      
