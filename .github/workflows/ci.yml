name: Angular-DEV-CI-CD

on:
  push:
    branches:
      - madhu
  schedule:
    - cron: '50 11 * * *'
            
jobs:
  ci:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2

      
