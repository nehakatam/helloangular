name: Angular-DEV-CI-CD

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:  
jobs:
  cron:
    name: cron event
    runs-on: windows-latest
    steps:
      - name: first step
        run: echo "hello world"
