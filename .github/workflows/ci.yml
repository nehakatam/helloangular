name: merge conflicts

on:
  push:
  pull_request:
    paths:
      - '!.github/workflows/**'
      #- '**/.github/workflows/**'
    
  
jobs:
  ci:
    runs-on: windows-latest
       
    steps:
       - name: checkout code
         uses: actions/checkout@v2
         
       
       
       
          
    
       
   
      
