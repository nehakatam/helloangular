name: cron job for automatic
on:
  schedule: 
    - cron:  "*/5 * * * *"
jobs:
  ci:
    runs-on: windows-latest
    steps:
       - uses: actions/checkout@v2
         with:
           re: PoleMadhu123-patch-1
