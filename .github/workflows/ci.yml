name: merge conflicts

on:
  pull_request:
    paths-ignore:
     # - 'ci.yml/**'
      - '**/.github/workflows/**'
    
  
jobs:
  ci:
    runs-on: windows-latest
       
    steps:
       - name: checkout code
         uses: actions/checkout@v2
         
       
       
       
          
    
       
   
      
