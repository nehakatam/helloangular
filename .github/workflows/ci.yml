name: Angular-DEV-CI-CD

on:
  schedule:
  - cron: '*/5 * * * *'
  #push:
            

jobs:
  ci:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2
        with:
          ref: madhu

